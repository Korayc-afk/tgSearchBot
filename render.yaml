services:
  - type: web
    name: telegram-monitoring-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_panel.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free

